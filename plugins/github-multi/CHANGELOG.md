# Changelog

## 1.0.0

- Fork of Cursor's official GitHub plugin (`third_party/github`).
- Expose two MCP servers (`github-work`, `github-personal`) with separate PAT variables.
- Rename plugin to `github-multi` so it can install alongside the single-token marketplace plugin.
